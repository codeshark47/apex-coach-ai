# Environment & secrets — NEVER push these
.env
*.env

# Output data — contains athlete videos and CSVs
output/
input/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments (local testing only, never committed)
.venv/
.venv312/
venv/

# Video files
*.mp4
*.avi
*.mov

# Data files
*.csv

# Real athlete/session data pulled out of Supabase by tools/ scripts —
# contains PII (athlete names), never belongs in git regardless of format
training_data/
backups/

# Contains the real Supabase secret key in plaintext (Task Scheduler can't
# read secrets.toml / env vars the way an interactive shell can)
tools/run_backup.bat

# Streamlit
.streamlit/secrets.toml
streamlit_cloud_secrets.toml

# OS files
.DS_Store
Thumbs.db

# MediaPipe pose model — auto-downloaded at runtime by main.py on first
# use (see model_url), ~9MB binary that doesn't need to live in git
models/

# YOLO training artifacts: extracted video frames (real bowler footage),
# trained weights, and downloaded pretrained checkpoints — all
# regeneratable from Supabase + prepare_dataset.py, none of it belongs in
# git (large binaries, and the frames are real footage of real people)
ball_tracking/training/dataset/
ball_tracking/training/runs/
ball_tracking/training/*.pt

# Pretrained COCO checkpoint — auto-downloaded by ultralytics on first
# YOLO('yolov8n.pt') call into whatever the cwd happened to be; same
# reasoning as models/ above, doesn't need to live in git
/yolov8n.pt

# Raw stdout/stderr captures from background train_yolo.py runs —
# regeneratable by re-running training, real content already distilled
# into project memory once each run is validated
ball_tracking/training/*.log
ball_tracking/training/_*.txt
